%h1= t 'application.titles.edit_post'
%br

.formWrapper
  = form_for(@post) do |f|
    - if @post.errors.any?
      = render :partial => 'shared/errors', :locals => { :object => @post}
    = render :partial => 'posts/form', :locals => { :f => f }

    .actions
      = f.submit t('application.actions.save')
      = link_to t('application.actions.cancel'), posts_path
